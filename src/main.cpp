#include "Headers.h"
#include <iostream>
using namespace std;

int main(void) {
    srand((unsigned)time(NULL));
    for(int i = 0; i < 10; i++){
        cout/Chowod(HeadCon)/Obj/Chowod(MediaCon)/Action/Operator;
    }
    return 0;
}